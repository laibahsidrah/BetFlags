package xyz.appmaker.pbyvul.ui.theme

import androidx.compose.ui.graphics.Color

/** Панели шапки / коэффициентов — тёмно-синий (букмекерский референс) */
val NavyPrimary = Color(0xFF0B2347)

/** Боковая колонка — ещё темнее */
val SportsbookSidebarBg = Color(0xFF060E1C)

/** Активная вкладка верхнего хаба */
val SportsbookTabActive = Color(0xFF0F6BAE)

/** Бейджи счётчиков, мелкие акценты */
val SportsbookAccent = Color(0xFF1FA2FF)

/** Золотой — вторичный акцент (редко) */
val GoldAccent = Color(0xFFFFD700)

/** Фон заголовков лиг / секций */
val SectionLeagueGrey = Color(0xFFF0F2F5)

/** Время матча LIVE (бирюзовый) */
val TealLiveTime = Color(0xFF00897B)

/** Тёмный блок под счётом */
val ScoreBlockNavy = Color(0xFF001A3D)

val OddsGold = GoldAccent

/** Ячейки коэффициентов — тёмная «кнопка» как на референсе */
val OddsCellBackground = Color(0xFF153A6B)

/** Чередование строк списка матчей */
val SportsbookRowAlt = Color(0xFFEEF2F7)

/** Основной текст на светлом фоне */
val TextPrimary = Color(0xFF1A1A1A)

/** Тёмные блоки LIVE (панели) */
val LiveSectionDark = Color(0xFF001A3D)

val PrimaryRed = Color(0xFFE53935)
val PrimaryRedDark = Color(0xFFC4141E)
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

/** Карточки и вторичный фон */
val LightGray = SectionLeagueGrey

val MediumGray = Color(0xFF9E9E9E)
val DarkGray = Color(0xFF424242)
val GreenLive = Color(0xFF4CAF50)
val YellowCard = GoldAccent
val RedCard = Color(0xFFF44336)

/** Счёт: цифры на тёмно-синем фоне */
val LiveScoreText = White
